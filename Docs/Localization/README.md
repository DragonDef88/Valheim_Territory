# Clan Territory language packs

Runtime language packs live in:

```text
BepInEx/plugins/ClanTerritory/Localization/<code>.txt
```

The mod creates `en.txt` and `ru.txt` automatically on first run. To add another language, copy one of those files, translate values, and set `Localization.Language=<code>` in the BepInEx config.
